# 兴享智家·慧家中枢复赛技术说明文档

## 1. 项目定位

本项目是面向三代同堂家庭的端侧家庭智能体 Demo，核心目标不是做泛聊天机器人，而是做一个本地家庭中枢：在不依赖外部云 API 的前提下，完成老人关怀、睡前场景联动、家庭网络诊断与 QoS、主动巡检、安全确认和家庭成员长期画像记忆。

系统采用“规则优先 + 本地 Qwen 小模型兜底 + 本地工具执行 + SQLite 长期记忆”的架构。规则路径保证主线场景稳定、低延迟、可解释；本地模型负责模糊表达识别和普通生活问题回答；SQLite 负责本地知识库和家庭成员长期画像持久化。

## 2. 运行入口与依赖

### 官方入口

比赛侧入口为根目录 `main.py`：

```python
def run(user_input: str, state: dict | None = None, config: dict | None = None) -> dict
```

实现链路：

- `main.py`：对外导出 `smart_home_agent.run`
- `smart_home_agent/__init__.py`：持有全局 `_AGENT = SmartHomeAgent()`
- `smart_home_agent/pipeline.py`：实现完整 Agent pipeline

### 依赖

配置文件：`pyproject.toml`

```toml
requires-python = ">=3.10"
dependencies = [
    "llama-cpp-python==0.3.19",
    "numpy<2.3",
]

[project.optional-dependencies]
demo = [
    "gradio>=4.0,<6",
]
```

本地模型文件：

- `smart_home_agent/data/qwen2.5-1.5b-instruct-q4_k_m.gguf`

本地数据库：

- `smart_home_agent/data/smart_home.db`

## 3. 总体架构

```text
用户输入
  ↓
LocalRouter
  ├─ 高优先级规则：确认 / 取消 / 长期画像记忆
  ├─ 主线场景规则：睡前、网络、提醒、巡检、设备等
  └─ 本地 Qwen 兜底：规则 unknown 或强制模型演示
  ↓
TaskPlanner
  └─ 将意图转换成确定性 DAG 步骤
  ↓
PlanExecutor
  └─ 按拓扑层执行工具，遇敏感动作进入确认队列
  ↓
ToolRegistry + Home Tools
  ├─ 设备 / 场景 / 网络 / 提醒 / 传感器 / 能耗
  ├─ SQLite 知识库
  └─ SQLite 家庭成员长期画像
  ↓
Pipeline Reply Renderer
  ├─ 工具结果模板化回复
  └─ unknown 或知识库无命中时调用本地 LLM 直接回答
  ↓
返回 reply / intent / plan / tool_results / state / metrics / safety
```

核心文件：

| 模块 | 文件 | 作用 |
|---|---|---|
| Pipeline | `smart_home_agent/pipeline.py` | 串联路由、规划、工具执行、回复生成、指标统计 |
| Router | `smart_home_agent/core/router.py` | 规则优先意图识别，本地 LLM 兜底分类 |
| Planner | `smart_home_agent/core/planner.py` | 意图到 DAG 工具步骤的确定性映射 |
| Executor | `smart_home_agent/core/executor.py` | DAG 拓扑执行，敏感动作中断确认 |
| Registry | `smart_home_agent/tools/registry.py` | 白名单工具注册与统一调用边界 |
| Home Tools | `smart_home_agent/tools/home_tools.py` | 家庭设备、网络、提醒、传感器、能耗等模拟工具 |
| Local LLM | `smart_home_agent/providers/local_llm.py` | llama-cpp-python 本地 Qwen 加载、分类、生成 |
| Session Memory | `smart_home_agent/memory/session.py` | 当前会话滑动窗口记忆 |
| Knowledge DB | `smart_home_agent/memory/sqlite_store.py` | SQLite 本地知识库检索 |
| Family Profile | `smart_home_agent/memory/family_profile.py` | 家庭成员长期画像持久化 |
| Demo UI | `demo/app.py` | Gradio 可视化演示界面 |

## 4. 返回结构

`run()` 返回：

| 字段 | 类型 | 说明 |
|---|---|---|
| `reply` | `str` | 面向用户的最终自然语言回复 |
| `intent` | `dict` | 意图名称、置信度、槽位、来源、识别理由 |
| `plan` | `list[dict]` | DAG 任务规划步骤 |
| `tool_results` | `list[dict]` | 每个工具的状态、数据、消息、耗时 |
| `state` | `dict` | 家庭状态、设备状态、传感器、提醒、画像、会话历史 |
| `metrics` | `dict` | 总延迟、NLU 延迟、模型耗时、工具耗时、内存估算 |
| `safety` | `dict` | 是否需要二次确认及原因 |

`metrics` 中额外记录模型真实性：

| 字段 | 说明 |
|---|---|
| `model_attempted` | 本轮是否尝试调用本地模型 |
| `model_available` | 本地模型是否成功加载可用 |
| `model_error` | 模型加载或调用错误 |
| `model_raw_output` | 意图分类阶段模型原始输出 |
| `answer_model_attempted` | 是否进入本地模型直接回答 |
| `answer_model_raw_output` | 直接回答阶段模型原始输出 |

## 5. 意图识别实现

意图识别位于 `smart_home_agent/core/router.py`。

### 5.1 规则优先

系统先执行规则路由 `_rule_route()`。规则覆盖比赛主线场景，确保演示稳定：

- 睡前 / 离家 / 观影场景
- 设备控制
- 家庭网络诊断
- 网络 QoS 多轮跟进
- 老人提醒创建与查询
- 主动巡检
- 传感器查询
- 能耗查询
- 儿童学习 / 睡眠模式
- 本地知识问答
- 长期家庭画像写入 / 查询
- 敏感动作确认 / 取消

### 5.2 受保护规则

以下意图不会被“调试：强制模型识别（仍走工具）”覆盖：

```python
PROTECTED_RULE_INTENTS = {
    "confirmation_accept",
    "confirmation_reject",
    "profile_memory_update",
    "profile_memory_query",
}
```

设计原因：

- 确认 / 取消属于安全链路，不能交给模型误判。
- “记住某人偏好”属于长期记忆写入，用户表达很明确时必须走确定性规则。

### 5.3 本地模型兜底分类与工具闭环

当规则识别为 `unknown`，且配置为 `mode="hybrid"`、`enable_local_llm=True` 时，会调用本地 Qwen 分类。

实现：

- `LocalRouter._local_llm_route()`
- `get_cached_local_llm_client(config)`
- `LocalLLMClient.classify_intent()`

模型只允许输出固定意图集合，系统会校验：

- 意图必须在 `ALLOWED_INTENTS`
- 置信度会归一化到 `0-1`
- 低于 `llm_min_confidence` 则不采用

本地模型在这里只负责“补识别意图”，不会直接绕过执行链路。只要模型识别出的意图能被 `TaskPlanner` 映射为工具计划，系统仍然按以下闭环执行：

```text
规则未命中 → 本地 Qwen 识别意图 → planner 生成 DAG → executor 调用工具 → pipeline 汇总工具结果生成回复
```

例如模型识别为 `home_status_query` 且槽位为 `room=老人房` 时，后续仍会调用 `home_profile.query`、`sensor.query`、`device.query`，最终回答来自传感器和设备工具结果，而不是模型自由生成。

### 5.4 直接回答兜底

直接回答不是主链路能力，只作为最后兜底。仅当以下情况出现时，pipeline 才会调用本地模型直接回答：

- 规则和本地模型都没有得到可执行意图，即最终仍为 `unknown`
- 意图存在但 `TaskPlanner` 没有对应工具计划
- `knowledge_query` 没有命中可信本地知识

- `SmartHomeAgent._llm_direct_reply()`
- `SmartHomeAgent._build_direct_reply_prompt()`

直接回答会读取：

- 当前传感器摘要
- 最近 6 条对话历史
- 长期家庭成员画像

这保证了类似：

```text
用户：怎么做可乐鸡翅
助手：给出步骤
用户：什么步骤
助手：承接上一轮，继续解释可乐鸡翅步骤
```

## 6. 任务规划与工具执行

任务规划位于 `smart_home_agent/core/planner.py`。

每个意图会映射为一个确定性 DAG。示例：

### 睡前模式

用户：

```text
爸妈准备睡了，帮我切到睡前模式，顺便检查一下门锁。
```

规划：

```text
s1 home_profile.query -> 确认目标家庭成员和房间
s2 scene.apply        -> 应用 sleep 场景
s3 device.query       -> 检查门锁
s4 reminder.query     -> 检查爷爷吃药提醒
```

执行：

- `PlanExecutor.execute()`
- `ToolRegistry.invoke()`
- `home_tools._scene_apply()`

### 网络诊断 + QoS

用户：

```text
爷爷房间视频有点卡，帮我看看。
```

规划：

```text
s1 home_profile.query
s2 network.diagnose
```

系统会在状态里写入：

```python
state["last_room"] = "老人房"
state["last_task"] = {"intent": "network_diagnose", "room": "老人房"}
```

用户继续：

```text
先保证爷爷那边。
```

路由会继承上一轮上下文，触发 `network_apply_qos`：

```text
s1 network.apply_qos
```

## 7. 功能清单与实现方式

### 7.1 场景模式联动

支持：

- 睡前模式
- 离家模式
- 观影模式
- 儿童学习模式
- 儿童睡眠模式

实现：

| 层级 | 实现 |
|---|---|
| 路由 | `LocalRouter._rule_route()` 识别“睡前 / 离家 / 观影 / 学习”等关键词 |
| 规划 | `TaskPlanner.build()` 生成 `scene.apply`、`device.query`、`reminder.query` |
| 工具 | `home_tools._scene_apply()` 修改设备状态 |
| 回复 | `SmartHomeAgent._reply()` 汇总场景执行结果 |

示例动作：

- 睡前模式关闭客厅电视
- 调暗客厅灯和老人房夜灯
- 主卧空调切换睡眠模式
- 检查门锁和老人提醒

### 7.2 设备控制

支持设备：

- 灯
- 电视
- 空调
- 门锁
- 摄像头

支持动作：

- 打开 / 关闭
- 调亮度
- 调温度
- 切模式
- 上锁 / 解锁

实现：

- 槽位抽取：`LocalRouter._extract_action()`
- 设备定位：`home_tools._find_device_id()`
- 设备修改：`home_tools._device_control()`

### 7.3 敏感动作二次确认

敏感动作：

- 解锁门锁
- 关闭摄像头
- 取消告警

实现：

| 步骤 | 文件 | 说明 |
|---|---|---|
| 规划安全门 | `planner.py` | 敏感动作先生成 `safety.check` |
| 拦截执行 | `home_tools._safety_check()` | 返回 `status="blocked"` |
| 挂起动作 | `executor.py` | 写入 `state["pending_confirmations"]` |
| 用户确认 | `router.py` | 识别 `confirmation_accept` |
| 执行动作 | `planner.py` | 读取 pending action 并补 `confirmed=True` |

示例：

```text
用户：帮我把门锁解锁
系统：需要二次确认
用户：确认执行
系统：执行 device.control unlock
```

### 7.4 老人关怀提醒

支持：

- 创建提醒
- 查询提醒
- 完成提醒
- 取消提醒
- 未响应后再次提醒参数

实现：

- 路由：`reminder_create` / `reminder_query` / `reminder_complete` / `reminder_cancel`
- 规划：`reminder.create` / `reminder.query` / `reminder.complete` / `reminder.cancel`
- 工具：`home_tools._reminder_create()`、`home_tools._reminder_query()`、`home_tools._reminder_complete()`、`home_tools._reminder_cancel()`

示例：

```text
提醒爷爷晚上九点吃降压药，如果十分钟没回应就再提醒一次。
```

结果：

```python
{
  "member": "爷爷",
  "time": "21:00",
  "task": "吃降压药",
  "retry_after_min": 10,
  "completed": False,
  "canceled": False
}
```

继续输入：

```text
爷爷已经吃过药了
取消爷爷今晚吃药提醒
```

系统会分别更新 `completed=True` 或 `canceled=True`，并在后续提醒查询中统计待完成、已完成和已取消数量。

### 7.5 家庭网络诊断与 QoS

支持：

- 房间网络质量诊断
- RSSI / 延迟 / 丢包率模拟
- Mesh 节点建议
- 高带宽设备识别
- 视频通话 QoS 策略
- 多轮上下文继承

实现：

| 功能 | 实现 |
|---|---|
| 网络诊断 | `home_tools._network_diagnose()` |
| QoS 应用 | `home_tools._network_apply_qos()` |
| 多轮继承 | `state["last_room"]`、`state["last_task"]` |
| 路由跟进 | `LocalRouter._rule_route()` 识别“先保证爷爷那边” |

QoS 后会更新：

- 房间状态变为 `optimized`
- 延迟降低
- 丢包率降低
- 客厅电视优先级降为 low

### 7.6 主动巡检

支持：

- 全屋传感器读取
- 老人房长时间无活动告警
- 老人房低温建议
- 儿童房夜间噪声建议

实现：

规划：

```text
s1 sensor.query
s2 sensor.check_alert
```

工具：

- `home_tools._sensor_query()`
- `home_tools._sensor_check_alert()`

规则样例：

- 老人房 `last_motion_min >= 120` 且无人活动，产生 warning
- 老人房温度低于 18℃，建议制暖到 24℃
- 儿童房夜间噪声大于 60dB，建议儿童睡眠模式

### 7.7 传感器查询

支持：

- 全屋温湿度查询
- 指定房间环境查询
- 活动状态查询

实现：

- 路由关键词：`温度`、`多少度`、`室温`、`冷不冷`、`湿度`、`有人吗`
- 规划：`sensor.query`
- 工具：`home_tools._sensor_query()`

示例：

```text
今天温度是多少
```

回复会返回全屋环境：

```text
全屋环境：客厅 24.5℃/55%；主卧 25.0℃/52%；老人房 17.5℃/60%；儿童房 25.0℃/50%
```

### 7.8 能耗分析

支持：

- 今日能耗总量
- 电费估算
- 设备耗电排行
- 节能建议

实现：

规划：

```text
s1 energy.query
s2 energy.optimize
```

工具：

- `home_tools._energy_query()`
- `home_tools._energy_optimize()`

节能建议示例：

- 空调今日耗电过高，建议睡眠时段切 ECO 模式
- 待机设备可关闭

### 7.9 儿童学习模式

支持：

- 儿童学习灯光
- 儿童房空调温度
- 45 分钟护眼休息定时器

实现：

- 路由：`child_mode_apply`
- 规划：`scene.apply` + `device.set_timer`
- 工具：`home_tools._scene_apply()`、`home_tools._device_set_timer()`

### 7.10 SQLite 本地知识库

知识库文件：

- `smart_home_agent/memory/sqlite_store.py`
- `smart_home_agent/data/smart_home.db`

默认知识包括：

- 老人睡前饮品建议
- 空调睡眠模式
- Wi-Fi 卡顿排查
- 睡前模式家庭规则
- 门锁安全策略
- 儿童护眼建议
- 家庭节电
- 空气质量与通风
- 老人防跌倒
- Mesh 部署
- 燃气安全

检索方式：

1. 如果启用 embedding，先尝试语义检索。
2. 否则使用关键词检索。
3. 关键词检索设置了最低门槛，避免仅凭“老人 / 睡前”等泛词乱命中。
4. 无可信命中时不伪造来源，而是转本地 LLM 直接回答或保守拒答。

回复会展示来源：

```text
根据本地知识库《老人睡前饮品建议》（keyword，score=12）：...
```

### 7.11 长期家庭成员画像

这是复赛迭代新增能力。

支持画像字段：

- 性格
- 爱好
- 饮食偏好
- 备注

实现文件：

- `smart_home_agent/memory/family_profile.py`

数据库表：

```sql
CREATE TABLE IF NOT EXISTS family_profiles (
    member TEXT PRIMARY KEY,
    personality TEXT NOT NULL DEFAULT '[]',
    hobbies TEXT NOT NULL DEFAULT '[]',
    diet TEXT NOT NULL DEFAULT '[]',
    notes TEXT NOT NULL DEFAULT '[]',
    updated_at TEXT NOT NULL
)
```

支持话术：

```text
记住爷爷喜欢吃清淡的，不吃辣，性格比较节俭。
记住妈妈喜欢看电影，性格比较急。
爷爷喜欢吃什么？
中午给家里吃什么好？
```

实现链路：

| 步骤 | 实现 |
|---|---|
| 识别写入 | `router.py` 中 `_is_profile_memory_update()` |
| 识别查询 | `router.py` 中 `_is_profile_memory_query()` |
| 任务规划 | `planner.py` 生成 `profile.remember` / `profile.query` |
| 工具写入 | `home_tools.register_home_tools()` 中 `profile.remember` |
| 持久化 | `FamilyProfileMemory.remember_from_text()` |
| 状态同步 | `state["family_profiles"]` |
| LLM 使用 | `_build_direct_reply_prompt()` 将长期画像注入 prompt |
| 前端展示 | `demo/app.py` 的“长期家庭画像”区域 |

特点：

- 存在 SQLite 中，重启后仍可读取。
- 前端提供“刷新长期记忆”按钮。
- 页面首次打开时自动加载已有画像。
- “调试：强制模型识别（仍走工具）”模式下，画像写入仍由规则优先保护，不会被模型误判成提醒。

### 7.12 会话短期记忆

实现文件：

- `smart_home_agent/memory/session.py`

机制：

- 每轮对话写入 `state["history"]`
- 默认保留最近 12 条
- 本地模型分类时读取最近上下文
- 本地模型直接回答时读取最近 6 条

解决问题：

```text
用户：怎么做可乐鸡翅
助手：给出做法
用户：什么步骤
助手：承接上一轮“可乐鸡翅”
```

### 7.13 本地 LLM 能力

实现文件：

- `smart_home_agent/providers/local_llm.py`

能力：

- 本地 GGUF 模型加载
- 意图分类
- 直接回答
- embedding 接口预留
- 模型状态查询
- 客户端缓存，避免每次重复加载模型

关键实现：

```python
get_cached_local_llm_client(config)
LocalLLMClient.classify_intent()
LocalLLMClient.generate_reply()
LocalLLMClient.status()
```

已验证依赖：

```powershell
python -m pip install "llama-cpp-python==0.3.19" --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu --prefer-binary
```

### 7.14 Gradio 前端演示

实现文件：

- `demo/app.py`

界面区域：

- 对话窗口
- 快捷场景按钮
- 模型策略切换
- 意图识别结果
- 本地模型状态
- 长期家庭画像
- 任务规划 DAG
- 工具调用日志
- 设备状态面板
- 传感器面板
- 能耗指标
- 性能指标

模型策略：

| 策略 | 说明 |
|---|---|
| 自动闭环（推荐） | 规则优先；规则未命中时由本地 Qwen 补识别意图；能规划则走工具；无工具或无可信知识时才直接回答兜底 |
| 规则仅调试（关闭模型） | 关闭本地模型，用于证明规则、工具、状态链路可独立运行 |
| 调试：强制模型识别（仍走工具） | 跳过普通规则识别，用于证明本地模型真实可用；识别出的意图仍进入 planner 和工具执行 |

长期画像区域：

- 首次打开自动加载 SQLite 画像
- 每轮 `chat_handler()` 后刷新
- 可手动点击“刷新长期记忆”

## 8. 当前支持的意图

| 意图 | 说明 |
|---|---|
| `scene_mode_apply` | 睡前 / 离家 / 观影等场景 |
| `device_control` | 设备控制 |
| `network_diagnose` | 家庭网络诊断 |
| `network_apply_qos` | 开启网络 QoS |
| `reminder_create` | 创建老人提醒 |
| `reminder_query` | 查询提醒完成状态 |
| `knowledge_query` | 本地知识库问答 |
| `home_status_query` | 家庭设备状态查询 |
| `child_mode_apply` | 儿童学习 / 睡眠模式 |
| `energy_query` | 能耗排行与优化 |
| `sensor_query` | 传感器查询 |
| `proactive_alert` | 主动巡检 |
| `profile_memory_update` | 写入长期家庭画像 |
| `profile_memory_query` | 查询长期家庭画像 |
| `confirmation_accept` | 确认敏感动作 |
| `confirmation_reject` | 取消敏感动作 |
| `unknown` | 未识别到确定任务，进入本地 LLM 直接回答 |

## 9. 当前注册工具

| 工具 | 类型 | 实现 | 说明 |
|---|---|---|---|
| `home_profile.query` | read | `_home_profile_query()` | 查询家庭成员、房间、设备关系 |
| `device.query` | read | `_device_query()` | 查询设备状态 |
| `device.control` | write | `_device_control()` | 控制灯、电视、空调、门锁等 |
| `device.set_timer` | write | `_device_set_timer()` | 设置定时动作 |
| `scene.apply` | write | `_scene_apply()` | 应用家庭场景 |
| `network.diagnose` | read | `_network_diagnose()` | 网络质量诊断 |
| `network.apply_qos` | write | `_network_apply_qos()` | 应用视频优先策略 |
| `reminder.create` | write | `_reminder_create()` | 创建老人提醒 |
| `reminder.query` | read | `_reminder_query()` | 查询提醒 |
| `reminder.complete` | write | `_reminder_complete()` | 标记提醒完成 |
| `reminder.cancel` | write | `_reminder_cancel()` | 取消提醒 |
| `knowledge.search` | read | `SQLiteKnowledgeBase.search()` | 本地知识库检索 |
| `safety.check` | gate | `_safety_check()` | 敏感动作安全门 |
| `sensor.query` | read | `_sensor_query()` | 查询传感器 |
| `sensor.check_alert` | read | `_sensor_check_alert()` | 主动巡检规则 |
| `energy.query` | read | `_energy_query()` | 能耗排行 |
| `energy.optimize` | write | `_energy_optimize()` | 节能建议 |
| `profile.remember` | write | `FamilyProfileMemory.remember_from_text()` | 写入长期画像 |
| `profile.query` | read | `FamilyProfileMemory.query()` | 查询长期画像 |

## 10. 状态设计

核心状态在 `home_tools.DEFAULT_HOME_STATE` 中定义。

主要字段：

| 字段 | 说明 |
|---|---|
| `family` | 家庭成员、成员组、房间与设备关系 |
| `devices` | 设备状态 |
| `network` | 房间网络质量、QoS、带宽设备 |
| `reminders` | 老人关怀提醒 |
| `sensors` | 温湿度、活动、噪声 |
| `energy` | 能耗数据 |
| `timers` | 定时任务 |
| `alerts` | 主动巡检告警 |
| `pending_confirmations` | 待确认敏感动作 |
| `family_profiles` | 长期家庭画像 |
| `history` | 短期会话历史 |

## 11. 性能与端侧特性

端侧特性：

- 无外部云 API 依赖
- 本地 GGUF 模型推理
- 本地 SQLite 存储
- 工具调用全部在进程内模拟
- 可断网运行核心流程

性能指标由 `pipeline.py` 统计：

- `total_latency_ms`
- `nlu_latency_ms`
- `model_latency_ms`
- `planning_latency_ms`
- `tool_latency_ms`
- `memory_mb`

规则路径通常为毫秒级；本地模型路径取决于机器 CPU 和模型缓存状态。

## 12. 演示脚本

| 脚本 | 作用 |
|---|---|
| `scripts/demo_live.py` | 主线场景离线演示 |
| `scripts/demo_local_llm.py` | 强制本地模型分类演示 |
| `scripts/demo_memory.py` | 短期多轮记忆演示 |
| `scripts/demo_family_memory.py` | 长期家庭画像演示 |
| `scripts/download_model.py` | 下载 GGUF 模型 |
| `scripts/run_demo.ps1` | PowerShell 运行 demo |

推荐复赛现场主脚本：

```text
1. 一句话定位：三代同堂家庭的本地家庭中枢
2. 睡前模式：场景联动 + 门锁 + 老人提醒
3. 老人房视频卡顿：诊断 + QoS 多轮跟进
4. 敏感动作：门锁解锁二次确认
5. 长期记忆：记住爷爷不吃辣，午餐推荐避开辣菜
6. 主动巡检：老人房低温 / 长时间无活动告警
```

## 13. 自动化验证

测试文件：

- `tests/test_scenarios.py`

覆盖内容：

- 睡前模式
- 网络诊断与 QoS 跟进
- 老人提醒与知识问答
- 知识库无命中拒答 / LLM 兜底
- 敏感动作二次确认
- 儿童学习模式
- 能耗查询
- 主动巡检
- 传感器温度查询
- 长期家庭画像持久化
- 强制模型模式下长期画像规则保护

运行：

```powershell
$env:PYTHONIOENCODING="utf-8"
python tests\test_scenarios.py
```

期望输出：

```text
all scenario tests passed
```

## 14. 配置项

默认配置位于 `smart_home_agent/settings.py`。

| 配置 | 说明 |
|---|---|
| `mode` | `hybrid` / `llm` |
| `enable_local_llm` | 是否启用本地模型 |
| `force_local_llm` | 是否强制模型分类演示 |
| `model_path` | GGUF 模型路径 |
| `n_ctx` | 模型上下文长度 |
| `n_threads` | 推理线程数 |
| `llm_temperature` | 模型温度 |
| `llm_max_tokens` | 最大生成长度 |
| `llm_min_confidence` | LLM 分类最低置信度 |
| `sqlite_path` | SQLite 数据库路径 |
| `max_steps` | 最大工具步骤数 |
| `require_confirm` | 是否开启敏感动作确认 |
| `enable_embedding_search` | 是否启用 embedding 检索 |

## 15. 创新点总结

### 15.1 规则优先与本地模型兜底结合

主线场景用规则保证稳定、可解释；模糊表达和普通生活问题用本地模型补足自然交互能力。

### 15.2 任务规划可解释

每轮输出 DAG 计划和工具日志，能清楚解释系统“为什么做、做了什么、状态如何变化”。

### 15.3 安全门机制

对门锁解锁等敏感动作做二次确认，避免模型或规则误触发危险操作。

### 15.4 长短期记忆结合

短期记忆解决多轮追问；长期画像解决家庭成员偏好、性格、饮食习惯的持久保存。

### 15.5 端侧可落地

本地模型、本地数据库、本地工具执行，无需云 API，符合隐私优先和端侧智能体方向。

## 16. 当前边界与后续优化

当前边界：

- 设备与传感器为模拟数据，未接真实 IoT 协议。
- 长期画像抽取使用规则，能覆盖复赛演示话术，但复杂自然语言可继续增强。
- 本地模型为 CPU 推理，首次加载和生成速度受机器影响。
- embedding 检索接口已预留，但默认关闭。

后续优化方向：

- 增加真实设备适配层，如 Matter / Home Assistant / MQTT。
- 用本地 LLM 辅助长期画像结构化抽取，但保留规则校验。
- 增加画像冲突处理，如“爷爷现在可以吃微辣了”覆盖旧偏好。
- 增加多用户会话隔离。
- 增加性能压测报告和复赛视频脚本。
