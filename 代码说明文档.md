# 代码说明文档

> 兴享智家·慧家中枢 — 端侧家庭智能体（2026 中兴捧月赛题作品）

## 一、官方入口

比赛规定入口为根目录 `main.py` 中的 `run` 函数：

```python
def run(user_input: str, state: dict | None = None, config: dict | None = None) -> dict
```

`algorithm/main.py` 也提供同名包装，便于评测脚本从 `algorithm` 目录直接导入。

返回字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `reply` | `str` | 自然语言回复（已渲染好） |
| `intent` | `dict` | `{name, confidence, slots, source, reasoning}` |
| `plan` | `list[dict]` | DAG 任务步骤列表 |
| `tool_results` | `list[dict]` | 每一步工具的执行状态、入参、产出 |
| `state` | `dict` | 完整家庭状态（设备 / 传感器 / 提醒 / QoS / 待确认队列等） |
| `metrics` | `dict` | 总延迟、NLU 延迟、模型延迟、工具延迟、内存估算 |
| `safety` | `dict` | 是否需要二次确认及说明 |

## 二、核心流程

```
用户输入 → LocalRouter（规则优先 + LLM 兜底）
        → TaskPlanner（意图 → DAG）
        → PlanExecutor + ToolRegistry（拓扑层执行 + 安全门）
        → SessionMemory + SQLiteKnowledgeBase（记忆 + 知识）
        → 自然语言回复
```

| 模块 | 文件 | 职责 |
|---|---|---|
| Router | [smart_home_agent/core/router.py](smart_home_agent/core/router.py) | 关键词/正则识别意图 + 槽位抽取；置信度低时调用本地 LLM 兜底 |
| Planner | [smart_home_agent/core/planner.py](smart_home_agent/core/planner.py) | 12 类意图各对应一个确定性 DAG 模板 |
| Executor | [smart_home_agent/core/executor.py](smart_home_agent/core/executor.py) | 拓扑层调度；遇 `safety.check` 拦截则压入待确认队列 |
| ToolRegistry | [smart_home_agent/tools/registry.py](smart_home_agent/tools/registry.py) | 白名单注册，隔离未授权调用 |
| Tools | [smart_home_agent/tools/home_tools.py](smart_home_agent/tools/home_tools.py) | 设备/场景/网络/能源/传感器/提醒/知识/安全共 14 个工具 |
| Memory | [smart_home_agent/memory/session.py](smart_home_agent/memory/session.py) | 短期对话窗口（默认 12 条） |
| Knowledge | [smart_home_agent/memory/sqlite_store.py](smart_home_agent/memory/sqlite_store.py) | SQLite 知识库；可选 Embedding 语义检索 + 关键词降级 |
| Provider | [smart_home_agent/providers/local_llm.py](smart_home_agent/providers/local_llm.py) | llama-cpp-python 本地推理（chat + embedding） |
| Pipeline | [smart_home_agent/pipeline.py](smart_home_agent/pipeline.py) | 串联以上模块并渲染回复 |

## 三、已实现工具

| 工具 | 类型 | 说明 |
|---|---|---|
| `home_profile.query` | read | 家庭成员、房间、设备关系 |
| `device.query` | read | 单设备或按房间过滤 |
| `device.control` | write | 灯/电视/空调/门锁/摄像头控制 |
| `device.set_timer` | write | 为设备设定 N 分钟后动作（如儿童护眼） |
| `scene.apply` | write | sleep / away / movie / **child_study / child_sleep** |
| `network.diagnose` | read | 模拟 RSSI/延迟/丢包/Mesh 节点诊断 |
| `network.apply_qos` | write | 视频通话优先策略 |
| `reminder.create` | write | 老人关怀提醒（含未响应自动重提醒） |
| `reminder.query` | read | 提醒完成状态 |
| `knowledge.search` | read | SQLite 知识检索（语义/关键词二选一） |
| `safety.check` | gate | 拦截解锁/关闭摄像头/取消告警等敏感动作 |
| `sensor.query` | read | **新** 全屋传感器（温/湿/活动/噪声） |
| `sensor.check_alert` | read | **新** 主动感知规则集 |
| `energy.query` | read | **新** 能耗 Top-N 排行 + 电费估算 |
| `energy.optimize` | write | **新** 自动识别待机设备 + 节能建议（支持 auto_apply） |
| `profile.remember` | write | **新** 长期家庭成员画像：性格 / 爱好 / 饮食 / 备注 |
| `profile.query` | read | **新** 查询长期家庭成员画像，并供本地模型回答时参考 |

## 四、支持的 12 类意图

`scene_mode_apply` · `device_control` · `network_diagnose` · `network_apply_qos`  
`reminder_create` · `reminder_query` · `knowledge_query` · `home_status_query`  
`child_mode_apply` · `energy_query` · `sensor_query` · `proactive_alert`  
`profile_memory_update` · `profile_memory_query`  
（另含 `confirmation_accept / confirmation_reject` 两个流程意图）

## 五、轻量化指标

| 维度 | 数值 |
|---|---|
| 默认运行依赖 | 仅 `llama-cpp-python`（可选）+ Python 标准库 |
| 演示依赖 | `gradio>=4.0`（仅 `demo/app.py` 使用） |
| 规则路径平均延迟 | 2–5 ms |
| 常驻 Python 内存（不含模型缓存） | ~72 MB |
| 模型权重（可选加载） | Qwen2.5-1.5B-Instruct Q4_K_M ≈ 0.95 GB |
| 网络依赖 | **零** — 全程端侧推理 |

## 六、本地模型真实性验证

当前项目采用“规则优先 + 本地 Qwen 兜底”的端侧 NLU。规则命中时不会调用模型；需要现场证明模型真实可用时，可运行：

```powershell
$env:PYTHONIOENCODING="utf-8"
python scripts\demo_local_llm.py
```

期望看到：

- `Intent: ... source= local_llm`
- `attempted: true`
- `available: true`
- `raw` 字段中包含本地模型输出的 JSON

Windows 环境建议使用已验证版本：

```powershell
python -m pip install "llama-cpp-python==0.3.19" --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu --prefer-binary
python -m pip install "gradio>=4.0,<6" "numpy<2.3"
```

如果模型不可用，前端和返回指标会显示 `model_attempted / model_available / model_error`，不再静默伪装成已接入模型。

## 七、长期家庭画像记忆

系统会把家庭成员画像写入本地 SQLite，重启 demo 后仍可读取。支持性格、爱好、饮食偏好和备注等轻量信息，并会在 LLM 兜底回答中作为上下文使用。

示例话术：

```text
记住爷爷喜欢吃清淡的，不吃辣，性格比较节俭。
记住妈妈喜欢看电影，性格比较急。
爷爷喜欢吃什么？
中午给家里吃什么好？
```

命令行演示：

```powershell
$env:PYTHONIOENCODING="utf-8"
python scripts\demo_family_memory.py
```

## 八、配置项（`config` 参数）

```python
{
    "mode": "hybrid",                  # rule / hybrid
    "enable_local_llm": True,          # 是否允许 llama.cpp 兜底
    "model_path": "...gguf",           # 默认 algorithm/smart_home_agent/data/
    "n_ctx": 1024,
    "n_threads": 4,
    "llm_temperature": 0.0,
    "llm_max_tokens": 256,
    "llm_min_confidence": 0.55,
    "force_local_llm": False,          # True 时用于答辩演示强制走本地模型
    "sqlite_path": "...smart_home.db",
    "max_steps": 8,
    "require_confirm": True,
    "enable_embedding_search": False,  # 启用后用 llama.cpp 做知识库 embedding
}
```
