# Known Issues

- 当前偏好记忆仍是进程内存，Agent 重建后会丢失。
- 外部运行时能阻止拒绝、ACK 不变和延迟故障被误报为成功，但拒绝与延迟目前统一归类为 `validation_failed`，详细原因需结合协议反馈传感器判断。
- 尚未完成 MQTT Broker、Home Assistant 和设备模拟器重启恢复矩阵。
- 自然语言反馈只覆盖节能场景的有限表达。
- 边缘模型双路由和完整 UI 工作台尚未实现。
- 团队情况、真实商业数据和现场答辩信息必须由用户提供，不能臆造。

