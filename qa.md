# 兴享智家·慧家中枢答辩问答 QA

> 用途：复赛答辩防守。问题围绕 PPT 主线、代码实现、`zhijia` 需求/接口/方案文档和比赛评分维度整理。回答建议现场根据时间压缩，不必逐字背诵。

## 一、产品定位与场景价值

### Q001：你们的作品一句话怎么介绍？
A：我们的作品是“兴享智家·慧家中枢”，面向三代同堂家庭的端侧家庭智能体。用户用自然语言下达家庭任务后，系统在本地完成意图理解、任务规划、工具调用、状态更新和自然语言回复，重点服务老人关怀、睡前联动、家庭网络诊断与 QoS、主动巡检和安全确认。

### Q002：为什么选择家庭场景，而不是校园或办公场景？
A：家庭场景最符合赛题推荐方向，也更容易体现端侧智能体价值。家庭里有设备割裂、老人关怀、网络卡顿、隐私保护和多轮状态延续等真实需求，这些都需要“理解场景 + 调工具 + 记状态”，不是单纯问答可以解决的。

### Q003：这个项目和普通智能音箱有什么区别？
A：普通智能音箱更多是指令转发或云端问答，我们强调本地闭环。系统会把一句话拆成 DAG 任务计划，调用设备、网络、提醒、传感器、知识库、长期画像等工具，并在界面展示意图来源、任务规划、工具日志和状态变化。

### Q004：你们为什么把产品定义为“本地家庭中枢”？
A：因为家庭数据包括作息、老人提醒、设备状态、网络日志和摄像头状态，隐私敏感。我们让本地模型、本地 SQLite、家庭状态和工具执行都在端侧完成，核心流程不依赖外部云 API，更适合部署在家庭网关、智能屏、轻量边缘盒子或路由器上。

### Q005：你们解决的核心痛点是什么？
A：核心痛点有四个：设备入口割裂，用户需要跨 App 操作；家庭网络问题普通用户难定位；老人提醒缺少上下文和二次确认；家庭数据上传云端有隐私风险。我们的系统用一个自然语言入口把这些任务统一起来。

### Q006：为什么聚焦三代同堂家庭？
A：三代同堂家庭有老人、父母、儿童三类角色，天然需要多场景协同。比如老人房网络视频卡顿、爷爷吃药提醒、孩子学习护眼、睡前全屋联动，这些任务都需要成员、房间、设备和状态之间的关系，能体现场景化智能体能力。

### Q007：这个作品的主价值是什么？
A：主价值是把家庭里的“理解、规划、执行、记忆、安全”串成闭环。它不是只会聊天，而是能执行家庭任务：比如切睡前模式、诊断老人房网络、创建吃药提醒、巡检异常、记住家庭成员偏好，并且每一步可以追踪。

### Q008：你们的商业应用前景在哪里？
A：可以作为家庭网关、路由器、智能屏、家庭机器人或运营商家庭宽带服务的本地 AI 入口。它可以降低家庭设备操作门槛，提供网络自愈、老人关怀和家庭安全能力，也能为终端厂商形成差异化智能服务。

### Q009：为什么不把它做成“什么都能聊”的机器人？
A：赛题强调场景化智能体、任务规划、工具调用和端侧落地。通用聊天很难证明执行能力，也容易答非所问。我们把主线收敛在家庭任务中枢，普通生活问答只作为最后兜底，主舞台是可验证、可演示的工具闭环。

### Q010：如果评委问“这是不是只是一个漂亮 Demo”，怎么回答？
A：我们的 Demo 不只是界面展示，后端有完整代码链路。入口是 `main.run()`，经过 `pipeline.py`、`router.py`、`planner.py`、`executor.py`、`ToolRegistry` 和 `home_tools.py`，真实更新内存状态和 SQLite 长期画像。前端只是把这些中间结果可视化出来。

## 二、赛题能力与评分维度

### Q011：你们如何对应“场景理解与任务规划”？
A：场景理解由 `LocalRouter` 完成，先用规则识别家庭主线意图，规则未命中时用本地 Qwen 兜底识别。任务规划由 `TaskPlanner` 完成，把意图转换为 DAG 步骤，例如睡前模式会生成家庭档案查询、场景应用、门锁检查和提醒查询。

### Q012：你们如何对应“工具调用与资源整合”？
A：系统通过 `ToolRegistry` 统一注册和调用本地工具，包括 `device.control`、`scene.apply`、`network.diagnose`、`network.apply_qos`、`reminder.create/query/complete/cancel`、`sensor.query`、`energy.query`、`knowledge.search`、`profile.remember` 等。

### Q013：你们如何对应“多轮对话与状态保持”？
A：短期状态保存在 `state["history"]`、`state["last_room"]`、`state["last_task"]` 和 `pending_confirmations` 中。比如先说“爷爷房间视频有点卡”，再说“先保证爷爷那边”，系统能继承上一轮网络诊断上下文，调用 `network.apply_qos`。

### Q014：你们如何对应“高效推理与实时响应”？
A：我们采用规则优先、本地模型兜底的混合推理策略。高频确定任务走规则路径，通常是毫秒级；只有规则不确定或需要直接回答时才调用本地 Qwen。模型客户端有缓存，避免重复加载和重复分类。

### Q015：你们对应的创新方向有哪些？
A：对应了轻量模型意图理解与槽位填充、端侧 Agent 架构设计与任务规划、本地知识库与检索增强、低资源环境下的持续学习与适应、场景感知和工具调用。长期家庭画像也是低资源适应能力的一种体现。

### Q016：如果按“创新”评分，你们怎么强调？
A：创新点在于不是单一智能家居控制，而是把家庭网络诊断、老人关怀、主动巡检、安全确认、长期记忆和本地模型补识别放进一个端侧 Agent 闭环。尤其是“规则优先 + 本地模型补识别 + 工具执行”的设计，兼顾稳定性和智能性。

### Q017：如果按“价值”评分，你们怎么强调？
A：价值在于真实家庭里这些问题高频存在：老人提醒、网络卡顿、睡前联动、孩子学习、用电管理。系统可接入家庭网关或智能屏，面向普通家庭、运营商和终端厂商都有应用前景。

### Q018：如果按“成本”评分，你们怎么强调？
A：我们避免重云端依赖，采用轻量 GGUF 本地模型、SQLite、本地工具和规则快速路径。核心功能可以在普通主机或轻量边缘设备上运行，模型是 1.5B 量化版本，资源成本远低于云端大模型全链路推理。

### Q019：如果按“演示”评分，你们怎么强调？
A：我们有可运行 Gradio Demo，不只是方案 idea。演示界面展示对话、快捷场景、运行策略、意图来源、本地模型状态、长期画像、任务规划 DAG、工具调用日志、设备状态、传感器面板、能耗和性能指标。

### Q020：如果按“文档”评分，你们怎么强调？
A：项目有 `zhijia/docs/01_需求文档.md`、`02_接口文档.md`、`03_实现方案效果.md`、`代码说明文档.md` 和 `复赛技术说明文档.md`。文档覆盖需求、接口、架构、功能、性能、演示和实现方式，和代码结构能对应上。

## 三、总体架构与执行链路

### Q021：系统的总体架构是什么？
A：总体链路是：用户输入 → `LocalRouter` 意图识别 → `TaskPlanner` 生成 DAG → `PlanExecutor` 执行工具 → `ToolRegistry` 调用本地能力 → 更新 `state` 和 SQLite → `Pipeline` 汇总工具结果生成回复 → 前端展示全过程。

### Q022：PPT 里的整体项目框架图怎么讲？
A：框架图分三层：交互层是真实可用的前端入口，包括用户、快捷场景、Gradio Demo 和 `main.run`；Agent 主链路是 Pipeline、Router、本地 Qwen、Planner、Executor、ToolRegistry、Reply Builder；本地能力层包括传感器、设备控制、场景联动、网络诊断、提醒、画像、知识库、安全确认、SQLite 和 State。

### Q023：`main.run` 的作用是什么？
A：`main.run(user_input, state, config)` 是比赛要求的统一入口。它对外隐藏内部复杂性，返回 `reply`、`intent`、`plan`、`tool_results`、`state`、`metrics` 和 `safety`，所以命令行、测试脚本和 Gradio 前端都可以复用同一个入口。

### Q024：`pipeline.py` 负责什么？
A：`pipeline.py` 是主流程编排。它负责合并配置、初始化工具、加载长期画像、调用 router、planner、executor，生成回复，更新短期历史，并统计总延迟、模型耗时、工具耗时和内存估计。

### Q025：`router.py` 负责什么？
A：`router.py` 负责意图识别和槽位抽取。它先用规则覆盖主线场景，包括睡前、网络、提醒、传感器、能耗、巡检、长期画像和敏感确认；如果规则未知且允许本地模型，就调用本地 Qwen 做兜底分类。

### Q026：`planner.py` 负责什么？
A：`planner.py` 把标准意图转换成确定性工具步骤。比如 `network_diagnose` 会生成 `home_profile.query` 和 `network.diagnose`；`home_status_query` 如果带房间，会生成 `home_profile.query`、`sensor.query` 和 `device.query`。

### Q027：`executor.py` 负责什么？
A：`executor.py` 按 DAG 依赖关系进行拓扑执行，并通过 `ToolRegistry.invoke()` 调用工具。它还负责安全中断，如果 `safety.check` 返回需要确认，就把待执行动作写进 `state["pending_confirmations"]` 并停止后续执行。

### Q028：`ToolRegistry` 的价值是什么？
A：`ToolRegistry` 是白名单工具边界。模型和路由不会直接操作设备，而是只能通过注册过的工具执行，这样能统一日志、统一调用参数、限制能力范围，也方便未来接入真实设备适配层。

### Q029：Reply Builder 是什么？
A：代码里对应 `SmartHomeAgent._reply()`。它不是固定一条回答，而是根据意图、工具结果、安全状态和当前 state 生成面向用户的自然语言回复。比如网络诊断回复会读取 RSSI、延迟、丢包率和诊断建议。

### Q030：为什么要设计 DAG，而不是顺序列表？
A：DAG 能表达依赖关系，方便解释“为什么先查档案再执行场景、为什么先诊断再 QoS”。当前 Demo 规模不大，但 DAG 结构为后续并发执行、失败重试和复杂任务编排保留了扩展空间。

## 四、本地模型与意图识别

### Q031：你们用的本地模型是什么？
A：默认配置使用 `qwen2.5-1.5b-instruct-q4_k_m.gguf`，通过 `llama-cpp-python==0.3.19` 在本地加载。配置路径在 `smart_home_agent/settings.py` 的 `model_path`，也支持用环境变量 `LOCAL_LLM_MODEL_PATH` 覆盖。

### Q032：为什么选 Qwen2.5-1.5B 量化模型？
A：1.5B 量级适合端侧演示，Q4_K_M 量化能降低内存和部署成本，同时中文理解能力足够完成意图分类和低风险生活问答。它比更大的模型更符合有限算力和低资源部署要求。

### Q033：本地模型在系统里负责什么？
A：本地模型有两类职责。第一是规则未命中时补识别意图和槽位，识别成功后仍然走 planner 和工具链。第二是在没有可执行工具或本地知识库无可信命中时，作为直接回答的最后兜底。

### Q034：本地模型会不会直接控制设备？
A：不会。模型最多输出固定意图和槽位，系统会校验意图必须在 `ALLOWED_INTENTS` 内，置信度必须超过阈值。只要能映射工具，后面仍由 `TaskPlanner` 和白名单工具执行，敏感动作还会进入安全确认。

### Q035：什么情况下会调用本地模型识别意图？
A：两种情况：自动模式下规则识别为 `unknown` 且配置为 `hybrid`、`enable_local_llm=True`；或者演示时选择“调试：强制模型识别（仍走工具）”，这时会跳过普通规则，用模型证明真实本地推理能力。

### Q036：什么情况下会调用本地模型直接回答？
A：只有最后兜底才直接回答：最终意图仍为 `unknown`、有意图但 planner 没有对应工具计划、或 `knowledge_query` 没有可信知识命中。直接回答不是主流程，主流程仍是工具闭环。

### Q037：为什么不所有输入都先走模型？
A：端侧资源有限，全部走模型会增加延迟和不稳定性。家庭主线任务需要可解释、可控和稳定，所以高频任务走规则路径；模型用于模糊表达补识别和普通问答兜底，这样更适合现场演示和端侧部署。

### Q038：模型输出不稳定怎么办？
A：系统限制模型只能输出 JSON，并且只接受固定意图集合。`router.py` 会校验 `name` 是否在 `ALLOWED_INTENTS`、置信度是否超过 `llm_min_confidence`。低置信或非法输出会被丢弃，不会进入执行链。

### Q039：`force_local_llm` 模式的意义是什么？
A：它是答辩演示用来证明“本地模型真的被调用”的调试模式。选择后，普通规则会让位给本地模型识别，但识别结果仍然进入 planner 和工具执行，不会变成纯模型自由回答。

### Q040：哪些规则不会被强制模型模式覆盖？
A：`PROTECTED_RULE_INTENTS` 保护确认/取消、提醒完成/取消、长期画像写入/查询等关键意图。原因是这些关系安全和持久化状态，必须用确定性规则保证不会被模型误判。

## 五、工具调用与功能实现

### Q041：系统当前注册了哪些工具？
A：注册工具包括家庭档案、设备查询/控制、场景应用、网络诊断、QoS、提醒创建/查询/完成/取消、安全检查、传感器查询、异常巡检、能耗查询、节能建议、设备定时、知识库搜索、长期画像写入和查询。

### Q042：睡前模式怎么实现？
A：路由识别 `scene_mode_apply`，规划生成 `home_profile.query`、`scene.apply`、`device.query`，如果是 sleep 场景再加 `reminder.query`。工具里 `scene.apply` 会关闭客厅电视、调暗客厅灯、主卧空调切睡眠模式、老人房灯调到 10%。

### Q043：离家模式怎么实现？
A：`scene.apply` 收到 `scene="away"` 后会关闭客厅电视、客厅灯、主卧灯，并执行门锁上锁，同时更新 `state["active_scene"]="away"`。这条链路证明场景不是文字描述，而是会修改设备状态。

### Q044：观影模式怎么实现？
A：观影模式由场景规则识别后调用 `scene.apply`，工具会打开客厅电视并将客厅灯亮度调到 15%，营造观影环境。状态变化会在设备状态面板中展示。

### Q045：儿童学习模式怎么实现？
A：`child_mode_apply` 会调用 `scene.apply`，把儿童房灯光调到 75%、空调调到 25℃，并通过 `device.set_timer` 设置 45 分钟护眼休息提醒。这对应儿童学习模式和低资源场景感知能力。

### Q046：家庭网络诊断怎么实现？
A：`network.diagnose` 读取 `state["network"]["rooms"]` 中的 RSSI、延迟、丢包率和高带宽设备。老人房默认 RSSI 为 -72 dBm、延迟 86 ms、丢包 3%，会生成 Mesh 信号偏弱、可开启 QoS、客厅电视占带宽的建议。

### Q047：QoS 是真实网络配置吗？
A：当前 Demo 是模拟工具，不直接修改真实路由器配置。`network.apply_qos` 会把 QoS 策略写入 state，把目标房间状态改为 `optimized`，降低模拟延迟和丢包率，并把客厅电视优先级调低。后续可替换为真实路由器 API。

### Q048：老人关怀提醒支持哪些动作？
A：支持创建、查询、完成和取消。代码里有 `reminder.create`、`reminder.query`、`reminder.complete`、`reminder.cancel`。提醒记录包含成员、时间、任务、是否完成、是否取消和未响应后再次提醒分钟数。

### Q049：提醒的“未回应再提醒一次”怎么体现？
A：创建提醒时会抽取 `retry_after_min`，例如“十分钟没回应”会写成 10，并存入提醒对象。当前 Demo 展示这个策略和状态，后续接真实通知系统时可以把它转换成实际定时触达。

### Q050：提醒完成怎么实现？
A：用户说“爷爷已经吃过药了”会被路由为 `reminder_complete`，planner 调用 `reminder.complete`，工具会筛选未完成提醒并写入 `completed=True`、`completed_at="now"`，回复会说明已完成的提醒。

### Q051：提醒取消怎么实现？
A：用户说“取消爷爷今晚吃药提醒”会触发 `reminder_cancel`，工具会把匹配的提醒写入 `canceled=True` 和 `canceled_at="now"`。之后查询时会统计待完成、已完成和已取消数量。

### Q052：主动巡检怎么实现？
A：`proactive_alert` 会规划 `sensor.query` 和 `sensor.check_alert`。工具读取全屋传感器，规则判断老人房 120 分钟以上无活动、温度低于 18℃、儿童房夜间噪声超过 60dB 等情况，并生成建议。

### Q053：传感器面板展示哪些数据？
A：传感器状态来自 `state["sensors"]`，包括房间、温度、湿度、是否有人活动、噪声和无活动分钟数。比如老人房是 17.5℃、湿度 60%、130 分钟无活动，这能触发老人关怀巡检建议。

### Q054：能耗分析怎么实现？
A：`energy.query` 会统计家庭今日总能耗、电费估算、阈值和设备耗电排行；`energy.optimize` 会生成节能建议，例如空调耗电较高时建议睡眠时段切 ECO 模式。

### Q055：设备控制支持哪些设备？
A：当前模拟设备包括客厅灯、客厅电视、主卧灯、主卧空调、老人房灯、老人房空调、老人房摄像头、儿童房灯、儿童房空调、入户门锁。支持灯光开关/亮度、空调温度/模式、电视开关、门锁上锁/解锁。

### Q056：设备状态是怎么被修改的？
A：工具函数直接修改 `state["devices"]` 中的模拟设备状态。比如 `turn_off` 会把 `power` 置为 `off`，灯还会把亮度置为 0；`set_temperature` 会修改空调温度；`unlock` 会把门锁 `locked` 置为 False。

### Q057：为什么说回复来自工具结果，而不是固定模板？
A：`_reply()` 会读取具体工具返回的数据来组织回复。网络诊断读取 RSSI、延迟、丢包率和 diagnosis；提醒查询读取提醒列表统计；家庭状态查询读取传感器和设备开关状态。模板只是表达形式，事实来自工具结果。

### Q058：工具失败怎么办？
A：工具返回统一结构：`status`、`data`、`message`。如果设备不存在、提醒没找到、传感器房间不存在，会返回 error，`_reply()` 会根据结果给出失败原因或保守回答。后续可以在 executor 层增加重试策略。

### Q059：为什么要把工具和规划分开？
A：规划负责“应该做什么”，工具负责“怎么执行”。这样模型或规则只产生意图，planner 生成标准步骤，工具层统一执行和记录，便于替换真实硬件、增加权限控制和做日志解释。

### Q060：当前工具是模拟的，怎么证明有落地潜力？
A：模拟工具使用了和真实接口类似的参数与返回结构，比如 `device.control(args, context)`、`network.apply_qos`、`reminder.create`。未来只需要把工具函数内部从修改 state 改成调用 Matter、MQTT、Home Assistant 或路由器 API，上层 Agent 链路不需要重写。

## 六、记忆、知识库与上下文

### Q061：系统有没有短期记忆？
A：有。`SessionMemory` 默认保留最近 12 条对话，写在 `state["history"]` 中。本地模型直接回答时会读取最近 6 条对话，所以像“怎么做可乐鸡翅”之后再问“什么步骤”，可以承接上一轮话题。

### Q062：系统有没有长期记忆？
A：有。长期家庭画像由 `FamilyProfileMemory` 实现，持久化在 SQLite 的 `family_profiles` 表中，字段包括 `personality`、`hobbies`、`diet`、`notes` 和 `updated_at`。重启后也可以加载。

### Q063：长期画像能记住什么？
A：能记住家庭成员的性格、爱好、饮食偏好和备注，比如爷爷不吃辣、喜欢清淡、性格节俭；妈妈喜欢看电影、性格急；孩子喜欢画画、喜欢恐龙等。

### Q064：长期画像怎么写入？
A：用户说“记住爷爷喜欢吃清淡的，不吃辣，性格比较节俭”会触发 `profile_memory_update`，planner 调用 `profile.remember`，工具再调用 `FamilyProfileMemory.remember_from_text()` 抽取并写入 SQLite。

### Q065：长期画像怎么查询？
A：用户问“爷爷喜欢吃什么”会触发 `profile_memory_query`，planner 调用 `profile.query`，从 SQLite 读取画像并格式化回复。前端右侧“长期家庭画像”区域也会自动刷新显示。

### Q066：长期画像为什么不用模型抽取？
A：当前演示优先稳定性和可解释性，所以用规则抽取覆盖答辩主话术。后续可以让本地 LLM 辅助抽取复杂表达，但仍要经过规则校验和结构化合并，避免模型写入错误记忆。

### Q067：长期画像和短期记忆有什么区别？
A：短期记忆保留最近对话，解决多轮追问；长期画像保存在 SQLite，解决跨会话的家庭成员偏好和习惯。比如“什么步骤”依赖短期记忆；“爷爷不吃辣”依赖长期画像。

### Q068：本地知识库是什么？
A：本地知识库由 `SQLiteKnowledgeBase` 实现，默认内置老人睡前饮品、空调睡眠模式、Wi-Fi 卡顿排查、睡前模式规则、门锁安全、儿童护眼、节电、通风、防跌倒、Mesh 部署、燃气安全等知识。

### Q069：知识库检索怎么避免答非所问？
A：关键词检索设置了 distinctive keyword 门槛，不能只凭“老人、睡前、家庭”这类泛词命中。必须命中特征词如浓茶、空调、网络、Mesh、门锁、护眼、节能、燃气等，并且分数达到阈值才返回。

### Q070：知识库没有命中怎么办？
A：如果禁用本地模型，就保守回复“本地知识库没有找到可引用的明确答案，我不会编造结论”。如果启用本地模型，则进入直接回答兜底，但不会伪造本地知识库来源。

### Q071：知识问答为什么不作为主舞台？
A：知识问答容易和通用聊天混淆，也容易出现误答。我们把它定位为辅助解释能力，主舞台是老人关怀、睡前场景、网络 QoS、主动巡检这些可执行工具闭环。

### Q072：可乐鸡翅这类普通问题怎么处理？
A：这类问题没有对应家庭工具，若本地模型可用，就作为直接回答兜底。系统 prompt 会带最近对话和长期画像，所以可以处理“怎么做可乐鸡翅”“什么步骤”这样的普通生活多轮问答。

### Q073：如果用户问实时天气、新闻或医疗诊断怎么办？
A：系统是端侧本地智能体，不依赖外部联网实时信息。直接回答 prompt 明确要求不编造外部实时信息和医疗诊断；需要权威来源时应说明本地无法确认，并给出可执行替代建议。

### Q074：状态会不会串会话？
A：当前 `run()` 支持传入 `state`，不同会话可以维护不同 state。长期画像保存在共享 SQLite 中，因为它代表家庭级记忆。后续如果需要多家庭隔离，可以给 SQLite 表增加 household_id 或 session_id。

### Q075：为什么前端以前画像不更新，现在怎么保证？
A：前端 `chat_handler()` 每轮会从 `result["state"]` 刷新 `_profile_summary(new_state)`，并且页面有“刷新长期记忆”按钮，首次打开也会从 SQLite 加载画像。这保证后端写入后前端能看到。

## 七、安全、隐私与可信执行

### Q076：敏感动作如何处理？
A：敏感动作包括解锁门锁、关闭摄像头、取消告警等。planner 会先生成 `safety.check`，executor 发现需要确认后把动作写入 `pending_confirmations` 并暂停执行，用户必须回复“确认执行”才会真正调用 `device.control`。

### Q077：为什么确认和取消不能交给模型？
A：因为确认/取消关系安全，不能被模型误判。代码里把 `confirmation_accept`、`confirmation_reject` 放入 `PROTECTED_RULE_INTENTS`，即使强制模型模式也不会覆盖这些规则。

### Q078：解锁门锁的完整流程是什么？
A：用户说“帮我把门锁解锁” → router 识别 `device_control` 且 action 是 `unlock` → planner 生成 `safety.check` → executor 挂起 pending action → 回复需要二次确认 → 用户说“确认执行” → planner 读取 pending action 并加 `confirmed=True` → 执行 `device.control unlock`。

### Q079：为什么说系统可解释？
A：每轮返回 `intent`、`reasoning`、`plan`、`tool_results`、`metrics` 和 `safety`。前端展示意图来源、规划 DAG、工具日志、设备状态和传感器状态，评委可以追问每个动作为什么发生、由哪个工具执行、结果是什么。

### Q080：隐私保护体现在哪里？
A：家庭画像、知识库、设备状态、传感器状态和提醒都在本地 state 或 SQLite 中处理；本地模型通过 GGUF 在进程内推理；核心流程不需要把家庭敏感数据发到外部云 API。

### Q081：如果本地模型不可用，系统还能运行吗？
A：能。规则路径、工具调用、状态更新、知识库检索和长期画像都可以独立运行。如果模型文件缺失，`LocalLLMClient.status()` 会记录错误，系统在规则路径下仍能完成主线演示。

### Q082：如果规则误判怎么办？
A：规则主要覆盖高频主线场景，并且有测试脚本覆盖。对于模糊表达，系统会在规则 unknown 时调用本地模型补识别；对于低置信模型结果不采用；对于敏感动作有安全确认拦截。

### Q083：为什么不让模型自由生成工具调用？
A：自由工具调用风险高，尤其家庭设备有安全隐患。我们让模型只做受限意图识别，工具选择由 planner 决定，执行由 registry 白名单控制，这样更可解释、更可控，也更适合比赛现场防守。

### Q084：工具调用日志有什么意义？
A：日志证明系统不是简单聊天，它真实经过了工具执行。每条日志包含工具名、状态、耗时和消息，能解释设备为什么变化、提醒为什么创建、QoS 为什么开启。

### Q085：你们有没有外部 API 依赖？
A：核心功能没有外部云 API 依赖。本地模型使用 `llama-cpp-python` 加载本地 GGUF 文件，知识库和画像使用 SQLite，家庭工具在进程内模拟。Gradio 只是本地 UI。

## 八、性能、端侧部署与成本

### Q086：端侧资源约束怎么满足？
A：架构上通过规则优先减少模型调用，用 1.5B 量化 GGUF 模型降低内存，用 SQLite 做轻量存储，工具在本地进程内执行。需求文档目标是不超过 6 TOPS、4GB 内存的端侧设备约束。

### Q087：当前性能指标怎么统计？
A：`pipeline.py` 统计 `total_latency_ms`、`nlu_latency_ms`、`model_latency_ms`、`planning_latency_ms`、`tool_latency_ms` 和 `memory_mb`。前端底部会展示这些指标，右侧模型状态会展示本轮是否调用本地模型。

### Q088：`memory_mb` 是真实系统内存吗？
A：当前是 Demo 级估算，不是操作系统级精确采样。它用于现场展示状态规模和相对资源占用。若要进入工程化评测，可以替换为 `psutil` 或平台监控接口获取真实内存。

### Q089：为什么本地模型有时延迟比较长？
A：本地模型是 CPU 推理，首次加载模型需要时间，生成式回答也比规则路径慢。我们通过客户端缓存、规则优先和分类缓存降低延迟，现场演示建议先预热模型。

### Q090：模型缓存具体做了什么？
A：`get_cached_local_llm_client(config)` 按模型路径、上下文长度、线程数、温度和最大 token 缓存客户端，避免每次请求重新加载模型。`LocalLLMClient` 还对分类输入做 `_classify_cache`。

### Q091：如何证明断网可运行？
A：核心链路不依赖云 API：规则、planner、tools、SQLite、本地模型文件都在本地。断网时只要依赖和模型文件已经安装，睡前模式、提醒、网络诊断、画像、知识库都可以运行。

### Q092：如果部署到真实开发板，需要改哪些部分？
A：主要改两类：本地模型推理后端可根据开发板替换为 NPU/AI 加速库；`home_tools.py` 的模拟工具替换为真实 IoT、路由器或传感器接口。上层 `run`、router、planner、executor 和前端展示可以保留。

### Q093：为什么使用 SQLite？
A：SQLite 轻量、无服务依赖、适合端侧持久化。它同时承担本地知识库和长期家庭画像存储，部署简单，断网可用，也方便后续迁移到嵌入式 Linux。

### Q094：你们如何降低成本？
A：降低成本的方式包括：本地规则处理高频任务，减少模型调用；使用量化小模型；使用 SQLite；核心工具本地执行；不依赖云端模型调用费用；真实落地时可复用家庭网关或路由器算力。

### Q095：当前并发能力如何？
A：当前 Demo 主要面向单家庭单会话展示。代码层 `run()` 是无状态入参加返回状态的形式，理论上多会话可以传不同 state；本地模型客户端有锁保护加载。工程化高并发需要进一步做会话隔离和队列调度。

## 九、Demo、测试与现场演示

### Q096：现场最推荐演示哪几条链路？
A：推荐六步：一句话定位；睡前模式；老人房视频卡顿诊断；多轮“先保证爷爷那边”开启 QoS；门锁解锁二次确认；长期记忆“记住爷爷不吃辣”后再做午餐推荐；最后一键巡检展示主动感知。

### Q097：快捷场景按钮有什么意义？
A：快捷场景不是偷懒，而是保证现场演示稳定触发主线能力。按钮填入的仍是自然语言输入，后端仍走同一个 `main.run` 链路，方便评委看到睡前、网络、巡检、儿童学习、能耗、提醒、安全等功能。

### Q098：运行策略三种模式怎么解释？
A：自动闭环是推荐模式：规则优先，规则未命中时本地 Qwen 补识别，能规划就走工具，无工具才直接回答。规则仅调试用于证明规则和工具链独立可运行。强制模型识别用于证明本地模型真实调用，但后续仍走工具闭环。

### Q099：测试覆盖了哪些场景？
A：`tests/test_scenarios.py` 覆盖睡前模式、网络诊断与 QoS、多轮提醒、知识问答、无命中保守拒答/LLM 兜底、提醒完成/取消、长期画像持久化、强制模型模式画像保护、安全确认、儿童模式、能耗、主动巡检、传感器查询和房间状态查询。

### Q100：如果专家问“你们当前最大的边界是什么”，怎么回答？
A：我们会诚实说明当前设备、网络和传感器是模拟数据，尚未接真实 IoT 协议；本地模型是 CPU 推理，速度受机器影响；长期画像抽取主要靠规则。我们的重点是证明端侧 Agent 闭环已经成立，后续可把模拟工具替换为真实硬件适配层，并增强多会话隔离和性能压测。

